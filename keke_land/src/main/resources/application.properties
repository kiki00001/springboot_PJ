# DB configuration -------------------------------------------------------------
# DB driver : MySQL config /Source URL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/db_name?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul 

# DB username/password
spring.datasource.username=root
spring.datasource.password=password

# true : JPA query check ok!
spring.jpa.show-sql=true

# DDL(create, alter, drop) method auto
spring.jpa.hibernate.ddl-auto=update 

# SQL legibility
spring.jpa.properties.hibernate.format_sql=true


# jsp config -------------------------------------------------------------
# spring.mvc.view.prefix=/WEB-INF/jsp/
# spring.mvc.view.suffix=.jsp




# thymeleaf config -------------------------------------------------------------
# Whether to enable a livereload.com-compatible server.
spring.devtools.livereload.enabled=true

# thymeleaf reference path
# Prefix that gets prepended to view names when building a URL.
# Suffix that gets appended to view names when building a URL.
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# thymeleaf cache configuration
# Whether to enable template caching.
spring.thymeleaf.cache=false 

# Whether to check that the templates location exists.
spring.thymeleaf.check-template-location=true

# server config -------------------------------------------------------------
# server.port = 8080